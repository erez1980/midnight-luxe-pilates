export const curatedVideoEmbeds: Record<string, { embedUrl: string; label: string }> = {
  // Only keep entries here after manual verification that embedding works.
  // Until then, exercises show a professional cover/image and instructions, not a fake video window.
};
